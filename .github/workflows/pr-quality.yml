name: pr-quality

on:
  pull_request:
  push:
    branches-ignore: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  PYTEST_SHARD_COUNT: 4

jobs:
  pr-lint-quality:
    name: pr-lint-quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86
        with:
          enable-cache: true

      - name: Install Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --dev

      - name: Ruff lint
        run: uv run ruff check .

      - name: Ruff format
        run: uv run ruff format --check .

      - name: Hosted security gates
        run: uv run python -m portfell.security_gates

      - name: Validate branch commit subjects
        run: uv run portfell-quality --commits-only

      - name: Validate squash merge subject
        if: github.event_name == 'pull_request'
        env:
          SQUASH_SUBJECT: ${{ github.event.pull_request.title }}
        run: uv run portfell-quality --squash-subject "$SQUASH_SUBJECT"

  pr-type-quality:
    name: pr-type-quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86
        with:
          enable-cache: true

      - name: Install Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --dev

      - name: Pyright
        run: uv run pyright

  pr-web-unit-quality:
    name: pr-web-unit-quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Install Web dependencies
        run: |
          docker run --rm --user "$(id -u):$(id -g)" \
            -e npm_config_cache=/tmp/npm-cache \
            -v "$GITHUB_WORKSPACE:/workspace" \
            -w /workspace/apps/web \
            node:24-alpine \
            sh -lc 'npm ci && npm run typecheck && npm run test:coverage'

  pr-web-interactions:
    name: pr-web-interactions
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Run deterministic browser interaction inventory
        run: |
          docker run --rm --ipc=host --user "$(id -u):$(id -g)" \
            -e CI=true \
            -e npm_config_cache=/tmp/npm-cache \
            -v "$GITHUB_WORKSPACE:/workspace" \
            -w /workspace/apps/web \
            mcr.microsoft.com/playwright:v1.62.1-noble \
            bash -lc 'npm ci && npm run build && npm run e2e'

      - name: Upload browser failure artifacts
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: pr-web-interactions-artifacts
          path: apps/web/test-results
          if-no-files-found: ignore
          retention-days: 7

  pr-real-stack-buttons:
    name: pr-real-stack-buttons
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Run real Docker browser button contracts
        run: bash scripts/run_real_stack_e2e.sh

      - name: Upload real-stack failure artifacts
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: pr-real-stack-buttons-artifacts
          path: apps/web/test-results
          if-no-files-found: ignore
          retention-days: 7

  pr-unit-tests:
    name: pr-unit-tests-${{ matrix.shard }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4]

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86
        with:
          enable-cache: true

      - name: Install Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --dev

      - name: Unit test shard
        run: >-
          uv run python scripts/pytest_shard.py
          --suite unit
          --shard-index ${{ matrix.shard }}
          --shard-count ${PYTEST_SHARD_COUNT}
          --
          -q
          -n auto

  pr-integration-tests:
    name: pr-integration-tests-${{ matrix.shard }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4]

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86
        with:
          enable-cache: true

      - name: Install Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --dev

      - name: Integration test shard
        run: >-
          uv run python scripts/pytest_shard.py
          --suite integration
          --shard-index ${{ matrix.shard }}
          --shard-count ${PYTEST_SHARD_COUNT}
          --
          -q
          -n auto

  pr-quality:
    name: pr-quality
    runs-on: ubuntu-latest
    needs:
      - pr-lint-quality
      - pr-type-quality
      - pr-web-unit-quality
      - pr-web-interactions
      - pr-real-stack-buttons
      - pr-unit-tests
      - pr-integration-tests
    if: always()

    steps:
      - name: Verify PR quality gates
        env:
          LINT_RESULT: ${{ needs.pr-lint-quality.result }}
          TYPE_RESULT: ${{ needs.pr-type-quality.result }}
          WEB_UNIT_RESULT: ${{ needs.pr-web-unit-quality.result }}
          WEB_INTERACTIONS_RESULT: ${{ needs.pr-web-interactions.result }}
          REAL_STACK_BUTTONS_RESULT: ${{ needs.pr-real-stack-buttons.result }}
          UNIT_RESULT: ${{ needs.pr-unit-tests.result }}
          INTEGRATION_RESULT: ${{ needs.pr-integration-tests.result }}
        run: |
          if [[ "$LINT_RESULT" != "success" || "$TYPE_RESULT" != "success" || "$WEB_UNIT_RESULT" != "success" || "$WEB_INTERACTIONS_RESULT" != "success" || "$REAL_STACK_BUTTONS_RESULT" != "success" || "$UNIT_RESULT" != "success" || "$INTEGRATION_RESULT" != "success" ]]; then
            echo "pr-quality failed: lint=$LINT_RESULT type=$TYPE_RESULT web_unit=$WEB_UNIT_RESULT web_interactions=$WEB_INTERACTIONS_RESULT real_stack_buttons=$REAL_STACK_BUTTONS_RESULT unit=$UNIT_RESULT integration=$INTEGRATION_RESULT" >&2
            exit 1
          fi
          echo "pr-quality passed."
