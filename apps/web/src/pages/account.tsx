import { Panel } from "../components/panel";

export function AccountPage() {
  return (
    <section data-route="account-page">
      <Panel title="Account">
        <p>Inspect identity and manage sign-out.</p>
      </Panel>
    </section>
  );
}
