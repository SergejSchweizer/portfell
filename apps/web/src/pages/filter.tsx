import { Panel } from "../components/panel";

export function FilterPage() {
  return (
    <section data-route="filter-page">
      <Panel title="Filter">
        <p>Apply reusable metric predicates to refine a selection.</p>
      </Panel>
    </section>
  );
}
