
import { Panel } from "../components/panel";

export function BivariateStatisticsPage() {
  return (
    <Panel title="Bivariate Statistics">
      <p>Compute and inspect pairwise statistics for the latest univariate-filter selection.</p>
    </Panel>
  );
}
